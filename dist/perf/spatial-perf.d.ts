export interface PerfSnapshot {
    enabled: boolean;
    fps: number;
    frameMsP50: number;
    frameMsP95: number;
    cullingMsP50: number;
    cullingMsP95: number;
    hitTestMsP50: number;
    hitTestMsP95: number;
    edgeHitMsP50: number;
    edgeHitMsP95: number;
    hitTestCallsPerSec: number;
    edgeHitCallsPerSec: number;
    visibleNodes: number;
    totalNodes: number;
    visibleEdges: number;
    totalEdges: number;
    virtualizationActive: boolean;
    seedVisibleNodes: number;
    nodesAddedByAdjacency: number;
    nodesAddedByEdgeEndpoints: number;
    edgesAddedByAdjacency: number;
    edgesAddedByCrossing: number;
    lastUpdatedAt: number;
}
type Listener = () => void;
declare class SpatialPerfStore {
    private enabled;
    private listeners;
    private lastTick;
    private lastRatesTs;
    private frameMs;
    private cullingMs;
    private hitTestMs;
    private edgeHitMs;
    private pendingCullingMs;
    private pendingHitTestMs;
    private pendingEdgeHitMs;
    private pendingHitTestCalls;
    private pendingEdgeHitCalls;
    private hitTestCallsPerSec;
    private edgeHitCallsPerSec;
    private visibleNodes;
    private totalNodes;
    private visibleEdges;
    private totalEdges;
    private virtualizationActive;
    private seedVisibleNodes;
    private nodesAddedByAdjacency;
    private nodesAddedByEdgeEndpoints;
    private edgesAddedByAdjacency;
    private edgesAddedByCrossing;
    private lastPublishedAt;
    isEnabled(): boolean;
    setEnabled(next: boolean): void;
    subscribe(cb: Listener): () => void;
    recordCulling(durationMs: number): void;
    recordHitTest(durationMs: number): void;
    recordEdgeHit(durationMs: number): void;
    setVisibilityCounts(data: {
        visibleNodes: number;
        totalNodes: number;
        visibleEdges: number;
        totalEdges: number;
        virtualizationActive?: boolean;
        seedVisibleNodes?: number;
        nodesAddedByAdjacency?: number;
        nodesAddedByEdgeEndpoints?: number;
        edgesAddedByAdjacency?: number;
        edgesAddedByCrossing?: number;
    }): void;
    tick(now?: number): void;
    getSnapshot(): PerfSnapshot;
    private emit;
}
export declare const spatialPerf: SpatialPerfStore;
export {};
