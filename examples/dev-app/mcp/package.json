{
  "name": "spatialboard-dev-app-mcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server (stdio) to drive the spatialboard dev-app in a browser for agents",
  "bin": {
    "spatialboard-dev-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "playwright": "^1.49.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
