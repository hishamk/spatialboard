# Dependencies
node_modules/
node_modules

# Build output (generated — `npm run build`; `prepack` rebuilds before publishing)
dist/

# Build caches
*.tsbuildinfo
.vite/

# Example app build output
examples/**/dist/

# Environment files hold secrets — keep *.example templates tracked
.env
.env.*
!.env.example
!.env.*.example

# Packaging + OS noise
*.tgz
.DS_Store
